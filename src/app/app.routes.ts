import { Routes } from '@angular/router';
import { HomepageComponent } from './homepage/homepage.component'; // Asegúrate de que el path sea correcto

export const routes: Routes = [
  { path: '', component: HomepageComponent }, // Página principal
  // Puedes agregar más rutas aquí si las necesitas
];
